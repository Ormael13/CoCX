package
{
	
	// splat in the const flag definitions and appearance definitions, so we can use them when 
	// everywhere.
	// It's horrible, yes, but at least they're all pretty damn unique, and sll
	// defined as const
	
	// include "../../includes/flagDefs.as";
	// include "../../includes/appearanceDefs.as";

	// aaaaand it doesn't work. Actionscript file include mechanisms..... I don't understand you.
}